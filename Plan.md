# Plan.md — план развития MapControl (штаб)

> Веду я (штаб). Каждый пункт — одно будущее задание кодовой сессии: отдельно сдаётся, отдельно откатывается.
> Статусы: `[ ]` — не начато, `[x]` — принято штабом. Номера заданий сквозные, выдаются при старте работ.
> Почта mail.ru — отдельный вопрос, сюда не входит.

## Принцип: сначала web, потом обёртка

Всё разрабатывается и тестируется как web-приложение (мне так доступно: HTTP, браузер, живые проверки).
Go+WebView — чистая упаковка без смены поведения, принимается вручную пользователем.

## Фаза 0 — Модель LLM: принята 3.7 (решением штаба без полного эталона)

- [x] 0001 — Golden-set отменён: 3.7 принята действующей по живой проверке (330 мс, валидный JSON, верные категория и сваи)
- [x] 0002 — Дефолт сменён на `qwen/qwen3.7-flash` (пример настроек + fallback в коде); живой конфиг меняет пользователь одной строкой

## Фаза 1 — Конфиг сайта в MapControl (без смены поведения)

- [x] 0003 — Вынести настройки сайта в конфиг (принято: живой прогон draft→update→check-text на 3.7, черновик удалён)
- [ ] 0004 — Фронт рисует форму из конфига: подписи, категории, обязательность свай (выдано кодовой сессии, диспетчер `docs/TASK.md`)
- [ ] 0004 — Фронт рисует форму из конфига: подписи, категории, обязательность свай

## Фаза 2 — Публикация в Zavodsvay-Static (чинится на месте)

- [ ] 0005 — `add-object`: прокинуть `pileCount` в собираемый объект
- [ ] 0006 — `add-object`: приём фото из пакета заявки с переименованием в `{id}_N.webp`

## Фаза 3 — PHP-приёмник в Zavodsvay-Static (замена почты)

- [ ] 0007 — POST-приём заявки по токену, очередь на диске
- [ ] 0008 — Мини-админка: список, просмотр, approve/reject с причиной, скачивание пакета; письмо только уведомление со ссылкой

## Фаза 4 — MapControl: отправка в приёмник + надёжность

- [ ] 0009 — HTTP-отправка в приёмник с очередью и ретраями, письмо как fallback
- [ ] 0010 — Быстрые фиксы из ревью: auth на shutdown, чистка темы письма, лимит пикселей sharp, сброс состояния LLM при смене черновика

## Фаза 5 — Desktop-обёртка

- [ ] 0011 — Go+WebView тонкая обёртка поверх текущего сервера: окно, иконка, запуск без консоли и браузера; поведения не меняет, приёмка вручную

## Фаза 6 — Второй сайт (только после фаз 1–4)

- [ ] 0012 — Клон конфига под второй сайт, проверка что абстракции хватило; при разрыве — решение штабом

## Заморожено / не делаем

- VDS под очередь — не нужен, приёмник живёт на PHP-хостинге рядом со статикой
- Новый репозиторий — не заводим, пока второй сайт не покажет что конфига мало
- Автосейв черновика, таймаут+ретраи LLM — ждут своей очереди после фазы 4 (не забыты)

# Plan.md — план развития MapControl (штаб)

> Веду я (штаб). Каждый пункт — одно будущее задание кодовой сессии: отдельно сдаётся, отдельно откатывается.
> Статусы: `[ ]` — не начато, `[x]` — принято штабом. Номера заданий сквозные, выдаются при старте работ.
> Почта — закрыта (site@zavodsvay.ru через kompleks-s.ru:465).

## Принцип: сначала web, потом обёртка

Всё разрабатывается и тестируется как web-приложение (мне так доступно: HTTP, браузер, живые проверки).
Go+WebView — чистая упаковка без смены поведения, принимается вручную пользователем.

## Фаза 0 — Модель LLM: принята 3.7 (решением штаба без полного эталона)

- [x] 0001 — Golden-set отменён: 3.7 принята действующей по живой проверке (330 мс, валидный JSON, верные категория и сваи)
- [x] 0002 — Дефолт сменён на `qwen/qwen3.7-flash` (пример настроек + fallback в коде + живой конфиг)

## Фаза 1 — Конфиг сайта в MapControl (без смены поведения)

- [x] 0003 — Вынести настройки сайта в конфиг (принято: живой прогон draft→update→check-text на 3.7, черновик удалён)
- [x] 0004 — Фронт рисует форму из конфига (принято: проверка в браузере, 9 категорий и центр из API)

## Фаза 2 — Публикация в Zavodsvay-Static (чинится на месте)

- [x] 0005 — `add-object`: прокинуть `pileCount` в собираемый объект (принято: dry-run 25/null/abc/-5)
- [x] 0006 — `add-object`: приём фото из пакета заявки с переименованием в `{id}_N.webp` (принято: dry-run с сортировкой, без записей)

## Фаза 3 — PHP-приёмник в Zavodsvay-Static (замена почты)

- [x] 0007 — POST-приём заявки по токену, очередь на диске (принято: живой прогон 200/401/список; админка пришла тем же файлом)
- [x] 0008 — Мини-админка: список, просмотр, approve/reject с причиной, скачивание пакета; письмо только уведомление со ссылкой (принято в составе 0007)

## Фаза 4 — MapControl: отправка в приёмник + надёжность

- [x] 0009 — HTTP-отправка в приёмник с очередью и ретраями, письмо как fallback (принято: живой прогон в стенд via inbox, файлы = мете)
- [x] 0010 — Быстрые фиксы из ревью: auth на shutdown, чистка темы письма, лимит пикселей sharp, сброс состояния LLM при смене черновика (принято: живой прогон 403/200/fail-fast)

## Фаза 5 — Desktop-обёртка

- [x] 0011 — Go+WebView тонкая обёртка поверх текущего сервера (принято: код + сборка, vet/gofmt чисто; запуск exe — вручную пользователем)

## Фаза 6 — Обёртка: доводка и второй сайт

- [x] 0012 — Иконка exe + видимая ошибка старта (принято: пин в exe, MessageBox вместо тихой смерти; визуальная проверка иконки — за пользователем)
## Фаза 7 — Публикация на сайте (выкатка)

- [ ] 0013 — Залить приёмник в прод по FTP + закрытый конфиг, проверка связи без приёма заявок (выдано, репозиторий Zavodsvay-Static)
- [ ] 0014 — Сквозной прогон: тестовая заявка в прод-очередь → уведомление → approve → скачивание пакета → add-object dry-run → reject (ждёт 0013)
- [x] 0015 — Drag&drop фото + заливка сразу при выборе + видимый GPS (принято: живой прогон в браузере — выбор, drop, GPS, проверка без дублей; плюс фикс штаба от повторной заливки)
- [ ] 0016 — Клон конфига под второй сайт, проверка что абстракции хватило; при разрыве — решение штабом (ждёт выбора сайта пользователем)

## Заморожено / не делаем

- VDS под очередь — не нужен, приёмник живёт на PHP-хостинге рядом со статикой
- Новый репозиторий — не заводим, пока второй сайт не покажет что конфига мало
- Автосейв черновика, таймаут+ретраи LLM — ждут своей очереди после фазы 4 (не забыты)

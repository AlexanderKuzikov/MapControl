# HANDOFF — продолжение сессии MapControl (2026-09-23)

Подписка кончилась посреди работ. Новая сессия: прочитай это, потом `docs/TASK.md`, `Plan.md`, `docs/CONTEXT.md` — и продолжай. После освоения файл удалить (коммит удаления).

## Где мы

Универсальный инструмент заявок на карту: операторское web-приложение (MapControl) + приёмник и публикация в Zavodsvay-Static. Я — штаб, кодовые сессии работают по файлам `docs/tasks/NNNN-*.md`. Модель LLM принята: `qwen/qwen3.7-flash` через RouterAI (проверена живьём). Почта закрыта: отправитель `site@zavodsvay.ru` через `kompleks-s.ru:465` SSL (пароли только в живом `.env`, в git их нет).

## Активные задачи (указатели, не пересказ)

- `docs/tasks/0011-desktop-webview.md` — открыта, репозиторий MapControl (обёртка; приёмка exe — вручную пользователем, последнее перед вторым сайтом).
- Принято и запушено: 0001–0010 (см. `Plan.md`, архив `docs/tasks/archive/`; 0007+0008 пришли одним файлом приёмника).

## Правила дороги (выстраданы за сессию)

- Коммиты прямо в `main`, без веток; сообщение `№NNNN: ...` у сессий, `Штаб: ...` у штаба. Пуш после каждой приёмки.
- Концы строк не менять: код LF, `docs/CONTEXT.md` CRLF. Проверять `git diff --stat` до коммита.
- Живой `.env` не трогать и не коммитить (он в игноре). `data/`, `node_modules/` не трогать.
- Фоновый сервер не переживает вызовы shell: весь живой прогон — одним заходом (старт, проверки, стоп, чистка черновика).
- Приёмка штаба — всегда живая (curl/браузер), отчёты сессий проверять по диффу, не на слово.
- Пароли и токены: в чат не светить, в репозитории не класть.
- Сайт по умолчанию — zavodsvay; отдельных приложений под сайты пока нет, только `config/site.json`.
- `gefestzavod@mail.ru` для этих задач не использовать (чужой). Получатель заявок не менялся.

## Хвосты

- favicon 404 в консоли (известный хвост из ревью, ни в одну задачу не входит).
- В `src/site-config.js` сообщение об ошибке говорит «скопируйте пример» — примера нет, мелочь для 0010 или позже.
- Подписка OpenCode кончилась; банк чинит карту 3–4 дня; мост — RouterAI по рублям (баланс ~237).
